{{#ui-dropdown class="selection" selected=this.activeRole.name onChange=(action this.setActiveRoleByName)}}
  <div class="default text">{{this.activeRole.name}}</div>
  <i class="dropdown icon"></i>
  <div class="menu">
    {{#each this.roles as |role|}}
      <div data-value={{role.name}} class="item">{{role.name}}</div>
    {{/each}}
  </div>
{{/ui-dropdown}}
